import { Orbitron, Genos } from 'next/font/google';

export const orbitronFont = Orbitron({
  subsets: ['latin'],
  display: 'swap',
});

export const genosFont = Genos({
  subsets: ['latin'],
  display: 'swap',
});
